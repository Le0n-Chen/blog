import bubbleSort from "./O(n^2)/bubbleSort";
import selectionSort from "./O(n^2)/selectionSort"
import insertionSort from "./O(n^2)/insertionSort"

import quickSort from "./O(nlogn)/quickSort";
import mergeSort from "./O(nlogn)/mergeSort";

const sorts = {
  bubbleSort,
  selectionSort,
  insertionSort,
  quickSort,
  mergeSort,
}

export default sorts; 